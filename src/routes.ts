/**
 *
 */
export const publicRoutes = ["/"];

export const authRoutes = ["/sign-in", "/sign-up", '/onboarding'];

export const apiAuthPrefix = "/api/auth"

export const protectedRoutes = ["/feed", "/setting"];
